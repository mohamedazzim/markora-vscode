# Themes

The document theme is independent from the VS Code shell theme. `follow-vscode` uses VS Code editor variables;
Markora presets provide Classic White, Paper, Academic, Sepia, Graphite, Midnight, and High Contrast. Themes are
implemented with document-scoped CSS variables and no external theme CSS is downloaded.
