# Release verification

Record the VS Code version, OS, VSIX SHA-256, install profile, extension activation, Reopen Editor With…, visual
editing, save, native source reopen, undo/redo, themes, math, Mermaid, image URI handling, uninstall, and
reinstall. A clean-profile result is not claimed until a real VS Code profile run is recorded here.
